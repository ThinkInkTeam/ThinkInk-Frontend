import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      devOptions: {
        enabled: true // optional, useful during dev
      },
      includeAssets: ['favicon.ico', 'robots.txt'],
      manifest: {
        name: 'ThinkInk',
        short_name: 'ThinkInk',
        description: 'ThinkInk is a platform for translating your thoughts and ideas to text.',
        theme_color: '#000',
        background_color: '#ffffff',
        display: 'standalone',
        start_url: '/',
        icons: [
          {
            src: '/pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: '/pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ]
})
